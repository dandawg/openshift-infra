apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cpu-machineset-aws-m6a-4xlarge
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/name: cpu-machineset-aws
    app.kubernetes.io/part-of: openshift-infra
    app.kubernetes.io/component: infrastructure
spec:
  project: default
  source:
    repoURL: https://github.com/dandawg/openshift-infra.git
    targetRevision: main
    path: infra/cpu-machineset/aws/helm
    helm:
      valueFiles:
        - values-m6a-4xlarge.yaml
      parameters:
        - name: clusterName
          value: REPLACE_ME  # Set via deploy script
        - name: region
          value: REPLACE_ME
        - name: availabilityZone
          value: REPLACE_ME
        - name: infraID
          value: REPLACE_ME
        - name: amiId
          value: REPLACE_ME
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-machine-api
  syncPolicy:
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 5m
  ignoreDifferences:
    - group: machine.openshift.io
      kind: MachineSet
      jsonPointers:
        - /spec/replicas
        - /spec/template/spec/providerSpec/value/blockDevices/0/ebs/volumeSize
